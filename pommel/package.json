{
  "name": "pommel",
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "build:codec": "wasm-pack build ../kitesabre-webcodec --target web --out-dir ../pommel/src/wasm/kitesabre_webcodec --out-name kitesabre_webcodec",
    "dev": "npm run build:codec && vite",
    "build": "npm run build:codec && vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "@vitejs/plugin-basic-ssl": "^2.3.0",
    "vite": "^8.1.0",
    "wasm-pack": "^0.13.1"
  }
}
